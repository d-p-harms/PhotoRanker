import SwiftUI

struct PrivacyPolicyView: View {
    var body: some View {
        ScrollView {
            Text("Your privacy policy goes here.")
                .padding()
        }
        .navigationTitle("Privacy Policy")
    }
}
